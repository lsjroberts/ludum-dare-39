{
  "scripts": {
    "start": "elm-live Main.elm --output elm.js --open",
    "compile": "elm make Main.elm --output elm.js || true",
    "cloc": "cloc --exclude-dir=$(tr '\\n' ',' < .clocignore) ."
  },
  "dependencies": {
  },
  "devDependencies": {
    "cloc": "^2.2.0",
    "elm-test": "^0.18.3"
  }
}
